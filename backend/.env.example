# Supabase Storage (`images` must be a public bucket for the stored URLs to render)
SUPABASE_URL="https://hblkugnxrwlelptrssnp.supabase.co"
# Server-only secret: never expose this in frontend code.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
